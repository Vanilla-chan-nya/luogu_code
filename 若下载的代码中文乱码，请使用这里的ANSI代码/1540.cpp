#include<iostream>
#include<cstdio>
#include<cstring>
#include<cmath>
#include<algorithm>
using namespace std;
int main()
{ 
	int m,n; cin>>m>n;
	int word,mem[m];
	memset(mem,-1,sizeof(mem));
	for(int i=0;i<n;i++) {
		cin>>word;
		bool flag=0;
		for(int j=0;j<m;j++)
			flag|=(word==mem[j]);
		if(flag) continue;
		if
		
		
	}
	
    return 0;
}
